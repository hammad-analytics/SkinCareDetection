{
  "name": "ai-skin-health-assessment",
  "private": true,
  "scripts": {
    "start": "run.bat",
    "dev": "concurrently -n \"ML,API,WEB\" -c \"magenta,cyan,green\" \"cd ml-service && .venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000\" \"node backend/src/server.js\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
